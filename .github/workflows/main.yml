name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up JDK 23
      uses: actions/setup-java@v2
      with:
        java-version: '23'
        distribution: 'temurin'

    - name: Build and Test with Maven
      run: mvn clean install

    - name: Run Tests
      run: mvn test
      env:
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

    - name: Upload JAR file
      uses: actions/upload-artifact@latest  # Updated to latest
      with:
        name: my-spring-boot-app
        path: target/Service-Ahead-version-5_0-0.0.1-SNAPSHOT.jar  # Adjust this path if your JAR file is named differently
